#!/usr/bin/python3


def number_keys(a_dictionary):
    """
    Returns the number of keys in
    'a_dictionary', assuming it's of type
    'dict'.
    """
    # the number of keys is the same as
    # its length
    # (and also its number of values)
    return len(a_dictionary)
