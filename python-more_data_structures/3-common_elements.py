#!/usr/bin/python3


def common_elements(set_1, set_2):
    """
    Returns a new set with only the items
    that show up in both set_1 and set_2
    (aka the sets' 'intersection')
    """
    return set_1.intersection(set_2)
