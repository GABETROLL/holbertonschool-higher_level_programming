-- Exercise 8: Count all the rows that contain
-- the value 89 in the 'id' column in the
-- 'first_table' table.
-- (and display the count in the console)
SELECT COUNT(*) FROM first_table WHERE id=89;